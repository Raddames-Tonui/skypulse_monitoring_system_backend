| Date                   | Phase                                     | Deliverables                                                                                                                                                                                                                                     | Key Tools / Concepts                              |
| ---------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| **🗓️ Day 1 (Nov 12)** | **Project Setup**                         | • Create repo structure: `backend/`, `frontend/`.<br>• Setup Maven `pom.xml`, Undertow base server, XML config, Hikari connection.<br>• Connect to Postgres, confirm DB connection pool works.<br>• Init Git + environment file templates.       | Java 17+, Undertow, HikariCP, Jackson, PostgreSQL |
| **Day 2 (Nov 13)**     | **Backend Architecture Skeleton**         | • Create package layout per module (`auth`, `monitoring`, `notifications`, `reporting`).<br>• Implement ConfigLoader (reads XML).<br>• Add global ResponseUtil (standard JSON replies).<br>• Implement common entities (BaseEntity, timestamps). | Hexagonal layout, DTOs, shared utils              |
| **Day 3 (Nov 14)**     | **Auth Module – User CRUD**               | • Create endpoints: `/users`, `/roles`.<br>• Implement password hashing (BCrypt).<br>• Add validation & ResponseUtil for errors.<br>• Test GET / POST users.                                                                                     | Undertow routing, JDBC/Hikari, BCrypt             |
| **Day 4 (Nov 15)**     | **Auth Module – Login & JWT**             | • Implement `/login` route.<br>• JWT generation + verification middleware.<br>• Add Captcha generator (6 random chars, track failed attempts per IP).                                                                                            | JWT, CaptchaUtil, Middleware pattern              |
| **Day 5 (Nov 16)**     | **RBAC & Permissions**                    | • Implement roles + permissions query.<br>• Add middleware for route-level authorization (based on role & CRUD flags).<br>• Secure user/role endpoints.                                                                                          | Role-based access control                         |
| **Day 6 (Nov 17)**     | **Monitoring Module – Schema Models**     | • Create Service entity mapping to DB.<br>• Implement repository for uptime checks & SSL checks.<br>• Implement scheduler placeholders.                                                                                                          | JDBC repositories, DAO pattern                    |
| **Day 7 (Nov 18)**     | **Uptime Check Logic**                    | • Implement HTTP ping job (retry, timeout, log results).<br>• Store results in `uptime_logs`.<br>• Detect transitions (UP→DOWN).                                                                                                                 | Executors, HTTP client, retry policy              |
| **Day 8 (Nov 19)**     | **SSL Monitoring**                        | • Implement SSL certificate fetcher & expiry calculation.<br>• Store results in `ssl_logs`.<br>• Emit “SSL Expiring” events.                                                                                                                     | `SSLSocket`, Cert APIs, LocalDateTime             |
| **Day 9 (Nov 20)**     | **Outbox & Notification Base**            | • Create `event_outbox` table + entity.<br>• Implement OutboxService (save events + worker thread for dispatch).                                                                                                                                 | Outbox Pattern                                    |
| **Day 10 (Nov 21)**    | **Notifications Module – Email/Telegram** | • Create NotificationService.<br>• Add Email (SMTP) + Telegram adapters.<br>• Add cooldown logic (skip duplicates in timeframe).                                                                                                                 | SMTP API, Telegram Bot API                        |
| **Day 11 (Nov 22)**    | **CQRS & Reporting Module**               | • Separate read models (queries) from command writes.<br>• Create report endpoints for uptime %, downtime counts.<br>• Materialized views / summary queries.                                                                                     | CQRS, SQL views, query optimization               |
| **Day 12 (Nov 23)**    | **Frontend Setup (React)**                | • Initialize React + Vite/CRA project.<br>• Setup routing + login page.<br>• Add environment config + API wrapper (axios).                                                                                                                       | React 18, React Router, axios                     |
| **Day 13 (Nov 24)**    | **Frontend Auth & Role Handling**         | • Login page (email/password/captcha).<br>• Store JWT in memory/localStorage.<br>• Role-based route protection.<br>• Logout flow.                                                                                                                | Zustand / Context, JWT handling                   |
| **Day 14 (Nov 25)**    | **Frontend Dashboard & Monitoring UI**    | • Create dashboard layout.<br>• Add service status tiles, uptime charts, SSL expiry table.<br>• Implement live updates via SSE.                                                                                                                  | React + SSE, Chart.js/Recharts                    |
| **Day 15 (Nov 26)**    | **Frontend Reports & Settings Pages**     | • Reporting page (uptime %, incidents).<br>• Settings page for retry intervals, thresholds.<br>• Contact group management.                                                                                                                       | Tables, forms, CRUD endpoints                     |
| **Day 16 (Nov 27)**    | **Testing & Integration**                 | • Test all CRUD endpoints end-to-end.<br>• Verify Captcha, JWT, RBAC, Outbox notifications.<br>• Run background jobs + confirm DB logs.<br>• Fix bugs, polish UI.                                                                                | Postman, Jest/React Testing                       |
| **Day 17-18 (Nov 28)** | **Deployment & Handover**                 | • Build Dockerfile for backend + frontend.<br>• Deploy to local or VPS.<br>• Verify config XML + env variables.<br>• Write setup + usage docs.                                                                                                   | Docker, Nginx, systemctl                          |
